# Private backend; use the same 32+ character random secret in both processes.
CURATOR_API_URL=http://127.0.0.1:8100
CURATOR_API_SECRET=
CURATOR_ORIGIN=http://localhost:3000
# Set only behind a trusted reverse proxy that overwrites X-Real-IP.
CURATOR_TRUST_PROXY=false
# Public URL of this deployment's corresponding source, required before publishing.
CURATOR_SOURCE_URL=
