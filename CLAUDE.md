# krystal-curator

Terminal screener + position monitor for Krystal LP pools (Robinhood chain first, USDG quote). No third-party indexer dependency: the removed rhpools feed must not come back — a future second source is a first-party chain feed.
Python 3.13, `uv`, Textual TUI. Owner is an LP / vault manager; numbers drive real capital.

## Commands
- `uv sync --all-groups` · `uv run pytest -q` · `uv run ruff check src tests && uv run ruff format src tests`
- `uv run krystal-curator` (TUI) · `scan` · `watch` (daemon) · `backtest` · `ask "question"` (agent addon) · `vault-review <URL>` (public vault → dated MD/JSON report in `reports/`) · `vault-leaderboard` (all public vaults: owners by ROI, copy candidates, `--review N` evaluates them; TUI `L` = `LeaderboardScreen`, Enter reviews one vault, verdicts cached on the app)
- Secrets in `.env` (gitignored): `KRYSTAL_WALLET`, optional `KRYSTAL_CLOUD_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`.
  Never commit `.env`, `exports/`, `reports/`.

## Layout (`src/krystal_curator/`)
- `api.py` `fetch_pools(source=…)` dispatch + Krystal LP-explorer feed (free) + Cloud API helpers (paid units) (the only feed; `reference_source()` returns None, so `reconcile.py` is dormant until a first-party chain feed exists) · `enrich.py` DexScreener logos/socials · `vaults.py` vault + strategy positions (free public API) · `positions.py` Position model + Cloud `/v1/positions`
- `net.py` the only HTTP door: `net.get/post/request` retry transport failures and 408/425/429/5xx with backoff + Retry-After (POST only when the request provably never arrived), raise `net.HttpError` with redacted text; `register_secret()` + `redact()` scrub the Telegram token and Cloud key from every error, toast and log (httpx/httpcore loggers filtered). Never call `httpx.get/post` directly outside net.py.
- `models.py` Pool + derived metrics · `profiles.py` risk profiles · `scoring.py` filters/score/grade/flags · `position.py` size simulator (dilution, σ²/8 IL) · `advisor.py` range-edge math · `rotation.py` opportunity cost · `analytics.py` track record / report
- `reconcile.py` deltas of the other feed vs the primary per pool id (`Reconciliation`, `Recon`); `api.fetch_reference_pools` picks the other feed; `Scored.recon` feeds the `SRCΔ` column / detail row · `risk.py` fills unknown σ / drawdown from snapshot price history (realised, 7d / 24h; sets `volatility_basis`) — runs in `Monitor.tick` after the snapshot and in `scan` · `store.py` sqlite (pool + vault snapshots, watchlist) · `monitor.py` alert rules shared by TUI and daemon · `daemon.py` `watch` loop · `notify.py` Telegram · `bot.py` Telegram commands · `autoconfig.py` Krystal Automation form values · `backtest.py` · `vault_review.py` one public AutoFarm vault from its URL: detail + `ai-agent-api.krystal.app/public-api` (`vault-agent-settings` = goal/instructions/permissions/restrictions/execution, `auto-farming-action-plans` paginated, `vault-agent-memories` 4 MB and ignores `limit` — unused) + `vaults/performance?timeframe=` series; `Review` snapshot with `served`/`missing`, `observe()` facts (no verdict), fixtures in `tests/fixtures/vault_review/` · `vault_eval.py` `Limits` (our goals as numbers) → `evaluate(review)`: `Performance` (closed pair series, fees vs price pnl, tx costs), `checks()` each pass/fail/unknown with basis `platform`/`instructions`/`data`, `Evidence` floor (14 d, 10 series), verdict by rule: platform fail → `avoid`, thin evidence → `insufficient_data`, fees pay for price moves → `worth_testing`, else `watch`; rendered into the same report · `leaderboard.py` `rank(vaults)` over `vaults.fetch_public_vaults(chain)` (paginated `/all/v1/vaults?chainIds=`, no strategies served): `VaultRank` (ROI = pnl / lifetime deposits, annualised; `why_not` rules → copy candidate), `OwnerRank` (Σ over all an owner's vaults, needs ≥ min_tvl deposited); candidates first, then the rest; fixture `tests/fixtures/vault_list.json`
- `agent/` local-LLM addon, off unless `[agent] enabled` (`config.Agent`): `server.py` `LlamaServer` spawns `llama-server` from the GGUF (or attaches to `base_url`), stops at exit · `llm.py` `run()` loop: per step one grammar-constrained JSON (`step_schema`: anyOf tool calls + final), identical call executed once, budget gone or same tool 3× → final-only schema (`forced_final`), `unverified_numbers` vs tool results, context compaction · `tools.py` `Toolbox` read-only tools (`limits`, `leaderboard`, `vault_review` via `compact_review`, `pool`, `my_positions`), `seed_review` avoids refetch · `tasks.py` `Session` (`review(rv, ev)`, `ask(q)`), `ROLE`, schemas, `AgentReport` duck-types `Evaluation` for `write_review(agent=…)`. Thinking off by default (`reasoning_budget = 0` → `chat_template_kwargs.enable_thinking=false`; per-request `reasoning_budget` is ignored by llama-server 0.4). Tests script the client (`tests/test_agent.py`).
- Web (`web/` Next.js + these modules): `web_api.py` FastAPI behind the Next bridge (`authorize` bearer, `market_user` session) · `web_auth.py` SIWE + Argon2, sessions, `/account` · `web_db.py` Tortoise models (users, sessions, preferences, watchlist, verdicts, pool samples, alert state, jobs, nonces) · `web_cache.py` per-key SWR cache (`get/peek/put`) · `web_feed.py` browser-fed data: recipes (the Krystal requests a dataset needs) + `ingest_*` in rounds into the per-user cache; GET endpoints answer `{needFeed}` / add `refreshFeed` via `serve()`; `CURATOR_SERVER_FETCH=true` restores server-side fetch — datacenter egress is Cloudflare-challenged by Krystal, never assume the server can reach `*.krystal.app` · `chain_rpc.py` live pool price from RPC (`slot0`, V4 `StateView`), `orient()` matches the feed's price orientation, `refresh_prices` rewrites `current_price`/`status` before alert rules · `web_rotation.py` rotation rows · `web_verdicts.py` verdict log, pool samples, `judge`, `track_record` · `web_alerts.py` Telegram alert loop (job lease, Monitor rules + verdict flips, RPC prices first) · migrations via aerich (`make web-migrate`).
- `tui.py` + `tui.tcss` all screens: CuratorApp (base = screener), PositionsScreen, TrackScreen, LeaderboardScreen (`a` = agent reading → `app.agent_runs`), AgentScreen (question box, `app.agent()` lazy Session, closed on unmount), SettingsScreen (`SETTINGS` spec → form → `config.save` + `app.apply_config`), HelpModal, `CuratorCommands` palette provider; `NAV` + `nav_text()` strip on every screen, `action_goto(name)` pops to base then pushes (digits are app bindings; SettingsScreen binds them to noop; an Input keeps them) · `cli.py` argparse entry

## Conventions
- `Pool.unknown` names metrics a feed did not provide (`volatility`, `drawdown`, `lp_auto`, `tvl`, `stat1h/7d/30d`); 0.0 there means unknown, never zero. Filters skip unknown metrics, scores treat them as neutral (0.5), flags that need them are not emitted. Any new rule reading those fields must check `unknown` first. Snapshots keep the feed's own values (unknown → 0.0), never the realised fill, so `backtest --sigma` compares feed vs realised, not realised vs itself.
- Pure logic lives outside `tui.py` with unit tests; TUI tests run on `tests/fixtures/top_pools_robinhood.json` with network patched out (`tests/test_tui.py`).
- Cloud API calls only on explicit user action (never on timers); count units in `units_used`.
- Widgets are looked up on the base screen (`self.main`) because modals sit on top; never name a method `_render`, `_closed`, `_open`, `size` on Textual subclasses (clashes with internals).
- API facts learned by probing (not in docs): `feeTier` is already a percent; v4 pools are keyed by 32-byte pool id in the LP explorer but `poolAddress` in vault strategies; vault `feeGenerated` includes pending fees; Krystal token `links` are empty for Robinhood tokens (use DexScreener); vault positions are not returned by Cloud `/v1/positions`.
- API facts verified by probing: `ageInSecond` on closed strategies is hold time (README "Assumptions"). Vault `apr`, strategy `apr` and performance-bucket `apr` are fractions (5.13 = 513 %; `vaults.py` converts to percent on parse); vault/strategy `maxTotalCost` = transaction costs spent; `maxValuePerStrategy` 0.15 with unit `%` = 15 % of TVL; `sharePriceUsd` is 0 on public vaults (no NAV/share series); vault discovery `GET /all/v1/vaults?chainIds=4663&isAutoFarmVault=true` (plural `chainIds`; `chainId` is silently ignored). Still unverified: `priceVolatility` treated as daily σ — `backtest --sigma` measures it from snapshot price history; token `usdPrice` is empty on Robinhood so `Pool.price0_usd/price1_usd` derive from `tvlTokenN / balanceN`.
